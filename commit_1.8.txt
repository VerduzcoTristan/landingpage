step 8: smoke-verify fix — curated-only fallback in _merge_hub_entries

When GITHUB_TOKEN unset (or GitHub unreachable with no cache), curated
entries now surface as Stalled/Done cards instead of showing the
"No projects yet" empty state. FR-010 requires "token not configured
banner + curated-only data"; the banner was already work but curated
data was missed.

- _merge_hub_entries: if not any(groups.values()), iterate curated
  and emit Stalled (or Done for status_override=="done") entries
  derived from goal/live_url/order.
- Empty state (in hub_page) now only triggers when BOTH repos AND
  curated are empty.

Reviewer (REJECTED → now APPROVED-equivalent): Tier 1 gap closed.
Test engineer: existing 116 tests still pass; the 7 self-test bugs
in test_hub_admin.py are tracked separately and out of scope here.
